"""
Make `hos-a2a/hos01` an importable Python package so the ADK loader can
`import hos01.agent` when `agents_dir` is the parent directory.

This file is intentionally empty other than the docstring.
"""
